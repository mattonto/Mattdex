/** * Represents the state of a Plandex plan. * This is a placeholder and should be expanded with actual plan properties. */export interface PlanState {  id: string;  name: string;  status: 'draft' | 'active' | 'completed' | 'archived';  createdAt: Date;  updatedAt: Date;}/** * Represents a conversation history, typically a sequence of messages. * This is a placeholder and should be expanded with actual message types. */export interface ConversationHistory {  id: string;  planId: string;  messages: Array<{    role: 'user' | 'assistant' | 'system';    content: string;    timestamp: Date;  }>;  createdAt: Date;  updatedAt: Date;}/** * Represents a context map, which might be a graph or tree of related information. * This is a placeholder and should be expanded with actual context map structure. */export interface ContextMap {  id: string;  planId: string;  nodes: Array<{ id: string; type: string; content: string }>;  edges: Array<{ id: string; source: string; target: string; type: string }>;  createdAt: Date;  updatedAt: Date;}/** * Represents a pending diff, capturing changes before they are applied. * This is a placeholder and should be expanded with actual diff structure. */export interface PendingDiff {  id: string;  planId: string;  targetEntity: 'PlanState' | 'ConversationHistory' | 'ContextMap';  entityId: string;  changes: Array<{    path: string;    op: 'add' | 'remove' | 'replace';    value?: unknown;    oldValue?: unknown;  }>;  createdAt: Date;}