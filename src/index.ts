import { Hono } from 'hono';import type { PlanState, ConversationHistory, ContextMap, PendingDiff } from './types';const app = new Hono();app.get('/health', (c) => {  return c.json({ status: 'ok', message: 'Local Persistence Manager is running' });});app.get('/api/plan-state/:id', (c) => {  const id = c.req.param('id');  const placeholderPlan: PlanState = {    id,    name: `Plan ${id}`,    status: 'draft',    createdAt: new Date(),    updatedAt: new Date()  };  return c.json(placeholderPlan);});export default app;export interface Env {}